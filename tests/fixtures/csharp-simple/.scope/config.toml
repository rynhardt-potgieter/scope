[project]
name = "csharp-simple"
languages = ["csharp"]

[index]
ignore = [
    "node_modules",
    "dist",
    "build",
    ".git",
]
include_tests = true

[embeddings]
provider = "local"
model = "nomic-embed-code"

[output]
max_refs = 20
max_impact_depth = 3
