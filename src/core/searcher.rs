//! Vector similarity search over symbol embeddings.
//!
//! Stub module — will be implemented when LanceDB integration is added.

/// Placeholder searcher struct.
#[allow(dead_code)]
pub struct Searcher;

#[allow(dead_code)]
impl Searcher {
    /// Create a new searcher (stub).
    pub fn new() -> Self {
        Self
    }
}
