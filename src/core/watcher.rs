//! File system watching for incremental re-indexing.
//!
//! Stub module — will be implemented when `--watch` mode is added.

/// Placeholder watcher struct.
#[allow(dead_code)]
pub struct Watcher;

#[allow(dead_code)]
impl Watcher {
    /// Create a new watcher (stub).
    pub fn new() -> Self {
        Self
    }
}
