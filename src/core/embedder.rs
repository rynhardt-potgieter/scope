//! Embedding generation for semantic search.
//!
//! Stub module — will be implemented when LanceDB integration is added.

/// Placeholder embedder struct.
#[allow(dead_code)]
pub struct Embedder;

#[allow(dead_code)]
impl Embedder {
    /// Create a new embedder (stub).
    pub fn new() -> Self {
        Self
    }
}
